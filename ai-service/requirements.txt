# ─────────────────────────────────────────────────────────────
# Agent Edu Automation — AI Service 依赖清单
# 跨平台最小化版本 (macOS / Linux / Windows 均可用)
# 推荐 Python 3.11.x (3.11.9 验证通过,不支持 3.12 / 3.13)
#
# 如需严格复现作者本地环境,参考 requirements.conda-freeze.txt
# ─────────────────────────────────────────────────────────────

# ── Web 框架 ──
fastapi>=0.115,<0.120
uvicorn[standard]>=0.30
starlette>=0.38
python-multipart>=0.0.9

# ── 配置 / 数据校验 ──
pydantic>=2.7,<3
pydantic-settings>=2.4
python-dotenv>=1.0
PyYAML>=6.0

# ── LangChain 生态 ──
langchain>=0.3,<0.4
langchain-core>=0.3,<0.4
langchain-community>=0.3,<0.4
langchain-openai>=0.2,<0.3
langchain-chroma>=0.1.4
langchain-ollama>=0.2
langchain-text-splitters>=0.3
langgraph>=0.2,<0.3
langsmith>=0.1.100

# ── LLM / Embedding ──
openai>=1.50,<2
httpx>=0.27,<0.29

# ── 向量库 / 检索 ──
chromadb>=0.5,<0.6
rank-bm25>=0.2.2
jieba>=0.42
flashrank>=0.2.9

# ── 文档解析(langchain_community loaders 依赖) ──
pypdf>=5.0
docx2txt>=0.8
unstructured>=0.15
markdown>=3.7
beautifulsoup4>=4.12

# ── 存储 ──
pymongo>=4.8,<5

# ── 日志 ──
structlog>=24.1

# ── 测试 ──
pytest>=8.0
pytest-asyncio>=0.24
