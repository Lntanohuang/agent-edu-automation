[pytest]
testpaths = tests
asyncio_mode = auto
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: 需要本地 Ollama 的集成测试 (用 -m "not integration" 跳过)
    slow: 耗时较长的测试 (用 -m "not slow" 跳过)
